# 0911 진행 기록

## 2026-09-11 KST 10:33 — 폴더 개설

지시: (1) 3일 학습 + 1일 테스트로 데이터 규모 변경, (2) 모델을 바꾸지 않는
진단 전부 진행.

### 즉시 판단이 필요한 것 두 가지 — 실행 전에 정한다

**판정 R39 (봉인 홀드아웃을 다시 정한다):**
  앞선 실험은 `20260317` 을 봉인했는데 이번 지시로 그 날짜가 학습에 들어간다.
  봉인이 사라지므로 **남은 날짜에서 새로 정한다 — `20260420` 이후 6거래일.**
  학습·테스트·진단 어디에도 쓰지 않고 마지막에 한 번만 연다.
  봉인이 필요한 이유는 그대로다: 여러 번 시험하면 우연히 좋은 결과가 나오고
  그것을 진짜라고 착각하게 된다.

**판정 R40 (GPU 사용을 재검토한다 — 앞선 R36 을 뒤집을 수 있다):**
  0910 에서 GPU 로 옮기지 않기로 했다(R36). 근거는 두 가지였다 —
  (a) 검증 사슬 전체가 CPU 결정론 위에 세워졌다, (b) 병렬화만으로 목표 시간에 닿았다.

  **이번에는 둘 다 흔들린다.**
  (a) 이번 실험은 **3일치·다른 분할**이라 0902/0909 의 검증과 애초에 직접 비교되지
      않는다. 비교 가능성 논거가 약해졌다.
  (b) 규모가 다르다. 0910 에서 교사 학습이 20만행 21분이었다. 저마찰층 3일치는
      대략 2,400만 행이고, 선형이면 **시드당 40시간**이 넘는다. 병렬화로 안 된다.

  **따라서 이번 실측(`SCALE.md`)에서 GPU 를 정면으로 평가한다.** 쓰기로 하면
  조건을 붙인다 — 결정론 설정을 최대한 켜고, **작은 규모에서 CPU 결과를
  재현하는지 먼저 확인**한 뒤에 본 실행에 들어간다. 재현이 안 되면 안 쓴다.

### 순서

```
1. 3일치 규모 실측 + GPU 판단      SCALE.md
2. 설계                            DESIGN.md
3. 성공·실패 기준 사전등록          PREREG.md   ← 결과 보기 전 커밋
4. 구현·실행
5. 결과·분석                       RESULTS.md, ANALYSIS.md
```

---

## 2026-09-11 KST 14:05 — 세션 중단 후 복구. 실측 결과 확보

이전 세션이 끝나며 실측 에이전트가 중단됐다. 작업물은 디스크에 남아 있었으나
**전부 미커밋**이었다(`gpu_eval/`, `scale/`). 여기서 커밋해 보호한다.
5M 행 측정은 `select()` 도중 끊겨 결과가 없다.

### 3일치 적재 실측

```
3일 총 행 수     70,001,560     적재 22.3분(하루 7.3분), 메모리 1.5GB
층별 3일치       저마찰 45,734,085 / 중간 17,844,717 / 고마찰 6,422,758
```

저마찰층이 4,570만 행 — 0910 교사가 실제로 본 19만 8천 행의 **230배**.

### ★ 표본 크기 대비 교사 R² 곡선 (핵심 진단)

| 학습 행 | ShallowMLP(CPU) R² | 수렴 | DeepLOB(GPU) R² | 수렴 |
| --- | --- | --- | --- | --- |
| 197,941 | 0.01595 | O | 0.01453 | **X 예산소진** |
| 495,046 | 0.01706 | O | 0.01405 | O |
| 990,113 | 0.01756 | O | 0.01744 | **X 예산소진** |
| 1,980,299 | 0.01816 | O | 0.01941 | **X 예산소진** |

R² 는 **안 본 종목**(선택 집합 198,079행)에서 잰 값이다.

**읽을 것:**
1. **데이터를 늘리면 R² 가 오른다 — 느리게.** 10배 늘려 얕은 모델 +14%(수렴값),
   DeepLOB +34%(하한값).
2. **얕은 모델과 DeepLOB 이 거의 같다.** 2M 행에서 0.0182 vs 0.0194. 큰 모델의
   이득이 작다 — "교사 용량이 병목이 아니다"는 0902 이래의 관찰과 부합.
3. **어느 쪽이든 0.02 근처다.** 수수료 23bp 를 이기려면 턱없이 모자라 보이지만,
   이것은 판정이 아니라 관찰이다 — 판정은 사전등록 후 본 실험에서 한다.

**⚠ 교란 — DeepLOB 곡선은 아직 결론을 못 낸다.** 4점 중 3점이 epoch 예산 300 을
다 쓰고도 조기 종료가 걸리지 않았다 = **아직 오르는 중에 끊겼다.** 이 값들은 하한이다.
0902 의 R22("탐색 예산 부족인지 방법 한계인지 구분 불가")와 같은 모양이다.
**예산을 늘려 수렴시킨 곡선을 다시 재기 전에는 "DeepLOB 은 데이터를 늘려도
안 된다"고 말하지 않는다.**

Ruling R41 (GPU 를 채택한다 — R36·R40 의 조건 충족):
  채택 조건은 "작은 규모에서 같은 시드로 CPU 결과를 재현하는가"였다.
  재현 기준을 여기서 명시한다: **최선 epoch 와 멈춘 epoch 가 같고, R² 차이가
  1e-4 미만**(우리가 재는 값 0.015~0.02 의 1% 미만, 판정을 뒤집을 수 없는 크기).

  | 시드 | 최선 epoch | 멈춘 epoch | R² CPU | R² GPU | 차이 | 속도 |
  | --- | --- | --- | --- | --- | --- | --- |
  | 0 | 60 / 60 | 160 / 160 | 0.034249 | 0.034290 | 4.1e-05 | 8.7배 |
  | 1 | 60 / 60 | 160 / 160 | 0.046525 | 0.046518 | 6.8e-06 | 9.8배 |

  두 시드 모두 통과. **학습 경로가 같은 해로 간다.** 비트 단위 재현은 포기하는 것이며,
  나중에 숫자를 추적할 때 GPU 비결정성이 후보로 남는다는 점을 기록해 둔다.

### ⚠ 디스크 100% (여유 64GB)

`/home/dgu` 가 7.0T 중 64GB 만 남았다. 대용량 캐시·중간 산출물을 쓰지 않는다.

---

## 2026-09-13 KST 23:59 — 머신 재부팅으로 수렴 곡선 재측정이 결과 없이 죽음

`b895a1a` 이후 새 커밋이 없다. 확인 결과:

- epoch 예산 1500 으로 다시 띄운 DeepLOB 곡선 측정(`deeplob_gpu_epochs1500`)이
  **첫 지점(20만 행) 도중**에 멈췄다. 로그 마지막 줄은 09-11 05:15 UTC
  `select() -> fit_rows=197,941`. 산출 JSON 없음 — **결과 0개.**
- **머신이 2026-09-11 06:52 에 재부팅됐다**(`uptime -s`). 세션 종료와 프로세스
  사망이 모두 이것으로 설명된다. 우리 코드의 오류 흔적(Traceback·OOM·CUDA 오류)은 없다.
- 재부팅은 `setsid`/`nohup` 분리 실행도 죽인다. 막을 방법은 없고 **지점별 체크포인트로
  잃는 양을 줄이는 것**이 유일한 대책이다. 스크립트는 이미 크기마다 저장하고
  끝난 크기를 건너뛴다 — 이번엔 첫 지점 도중이라 잃은 것이 전부였다.
- **디스크가 풀렸다:** 100%(여유 64GB) → 76%(여유 1.7TB).

→ 수렴 곡선을 다시 띄운다. 에이전트가 세션과 함께 연달아 죽었으므로 **코디네이터가
  직접 분리 실행**하고 크기마다 커밋한다.

### KST 00:05 — 코디네이터가 직접 재기동 (크기마다 자동 커밋)

에이전트가 세션과 함께 두 번 죽었으므로 코디네이터가 직접 띄웠다.
`scale/run_detached.sh` — 파이썬 실행을 감싸고, 결과 JSON 이 갱신될 때마다
**즉시 커밋**한다(1분 주기 확인, 두 실행이 같은 잠금 파일로 커밋 충돌을 피한다).
재부팅이 또 오면 진행 중이던 지점 하나만 잃는다.

| 실행 | 장치 | 설정 | 목적 |
| --- | --- | --- | --- |
| DeepLOB 수렴 곡선 | GPU | epoch 1500, 20만~1000만 6지점 | 지난번 300 epoch 는 4점 중 3점이 오르는 중에 끊겼다. 끝까지 수렴시킨다 |
| 얕은 모델 곡선 연장 | CPU | 기존 4점과 **같은 설정**(epoch 700, 평가 10, patience 10) | 500만·1000만을 추가해 두 모델을 같은 지점에서 비교한다. 끝난 4점은 건너뛴다 |

두 실행 모두 PID 1 로 재부모화 확인. 둘 다 3일치 풀 적재(약 22분)부터 시작한다.

### KST 00:08 — 수렴 곡선 첫 지점: 지난번 값은 실제로 잘린 하한이었다

| 20만 행 DeepLOB | epoch 예산 | 최선 epoch | 멈춘 epoch | 조기 종료 | 안 본 종목 R² |
| --- | --- | --- | --- | --- | --- |
| 지난번 | 300 | 270 | 300 | **X (예산 소진)** | 0.01453 |
| 이번 | 1500 | 340 | 440 | **O (수렴)** | **0.01789** |

**예산을 늘려 끝까지 학습시키니 R² 가 23% 올랐다.** 지난번 값은 오르는 중에 끊긴
하한이었다는 것이 확인됐다. 재측정을 결정한 근거(0902 의 R22 와 같은 모양)가 맞았다.

같은 20만 행에서 얕은 모델(수렴)은 0.01595 — **수렴시킨 DeepLOB 이 약 12% 높다.**
지난번 잘린 곡선으로는 "두 모델이 거의 같다"로 읽혔는데, 그 해석은 보류한다.
나머지 지점이 나와야 판단한다.

재측정이 지난번보다 훨씬 빠르다(얕은 모델 500만 행 표본 선택 4,908초 → 175초).
지난번에는 다른 무거운 작업과 CPU 를 다퉜다(부하 123). 시간은 산출물이 아니므로
결과에는 영향 없다.

---

## KST 00:20 — 종목군 분석 준비: 두 가지 발견

### 발견 1 — 호가 단위표를 외워 쓰면 틀린다

종목군을 LOBFrame 논문처럼 **상대 틱 크기**(스프레드가 평균 몇 틱인가)로 나누려면
틱 크기가 정확해야 한다. 기억에 의존하지 않고 데이터에서 쟀다
(무작위 160개 파일, 1~5호가 인접 가격 간격의 가격대별 최빈값).

```
가격대        최빈 간격 (상위 3)
<2천          1원 77%,  2원 10%,  3원 4%
2천~5천       5원 83%,  10원 10%, 15원 3%
5천~2만       10원 52%, 5원 31%,  20원 6%
2만~5만       50원 52%, 5원 24%,  10원 5%
5만~20만      100원 45%, 5원 37%, 200원 3%
20만~50만     500원 99%
≥50만         5원 100%
```

가격대별 최빈값은 호가 단위표(1·5·10·50·100·500원)와 맞는다. 그런데 **높은 가격대에
5원 간격이 24~37% 섞여 있고 50만원 이상은 100% 5원이다.** 이 표본은 전체 3,726 파일에서
뽑아 ETF 등이 섞였으므로 **ETF 의 다른 호가 규칙**일 가능성이 크다 — 단, 추정이다.
2,507개 주식 종목만으로 다시 재면 확인된다(아래 측정의 `tick_share` 로 드러난다).

**판정:** 표를 코드에 박지 않는다. **종목마다 실제 인접 호가 간격의 최빈값을 틱으로
쓴다.** ETF 규칙이든 가격대 경계를 넘나드는 종목이든 자동으로 맞는다. 최빈값이 차지하는
비율(`tick_share`)을 함께 저장해 틱 판정이 불확실한 종목을 걸러낼 수 있게 한다.

### 발견 2 — 곡선 측정은 모델도 예측값도 저장하지 않는다

`sample_size_curve.py` 는 R² 를 계산하고 모델과 행별 예측값을 버린다.
**종목군별 R² 를 내려면 종목 식별자와 함께 행별 예측값을 저장하는 별도 실행이 필요하다.**

### 설계상 짚어둘 차이

지시는 **"3일 학습 → 다음 하루 전체로 테스트"** 다. 그런데 표본 크기 곡선은
**같은 3일 안에서 학습에 안 쓴 종목(중간 마찰층)** 으로 R² 를 잰다 — 종목 축 일반화다.
날짜 축 일반화(`20260319`)는 아직 한 번도 안 쟀다. 곡선은 "데이터를 늘리면 오르는가"
라는 진단용으로 두고, **종목군 분석과 본 실험은 지시대로 다음 날(`20260319`)에서 평가한다.**

### 지금 돌리는 것

2,507개 주식 종목의 종목군 기준값 측정(`groups/symbol_covariates.py`, 기준일 `20260316`):
종목별 틱, 스프레드 평균 틱 수, 1틱에 붙어 있는 시간 비율, 거래대금, 호가 갱신 수.
**종목군 경계는 이 분포만 보고, 종목군별 R² 를 보기 전에 정한다.**

---

## KST 00:30 — 종목군 기준값 측정 결과 (2,507/2,507, 50초)

**틱 판정은 주식 종목에서 깨끗하다.** 최빈 간격 비율(`tick_share`) 하위 5% 가 0.591.
앞서 전체 파일 표본에서 본 5원 간격 혼입은 **ETF 였다는 추정이 뒷받침된다.**

**기존 마찰층과 상대 틱은 다른 축이다.**

```
마찰층   스프레드 평균 틱 수   1틱 고정 시간   상대 틱(bps)
L          1.633               0.613          10.5
M          2.277               0.382          13.4
H          3.345               0.208          15.3
스프레드(bps) 와 상대 틱(bps) 순위상관: 0.331
```

- 저마찰층(L)이 오히려 **스프레드가 1.6틱에 붙은 "큰 틱" 종목**이다. 고마찰층(H)은 3.3틱에 걸친다.
- 순위상관 0.33 — 마찰(bps 스프레드)을 틱 축으로 재사용했다면 다른 것을 쟀을 것이다.
  사전등록에서 **상대 틱을 1차 축으로 따로 둔 판단이 맞았다.**

사전등록 규칙(3분위)을 기계적으로 적용한 경계·제외 수·교차표는
`groups/data/group_boundaries.txt`, 종목별 배정은 `groups/data/group_assignment.csv`.
**종목군별 R² 는 아직 한 번도 보지 않았다.**

---

## KST 00:45 — 표본 크기 곡선 중간 결과와 새 함정: "조기 종료 걸림 ≠ 수렴"

| 학습 행 | DeepLOB 수렴(1500) | DeepLOB 잘림(300) | 얕은 모델 |
| --- | --- | --- | --- |
| 20만 | **0.01788** (최선 340, 멈춤 440) | 0.01453 잘림 | 0.01595 |
| 50만 | **0.01405** (최선 80, 멈춤 180) ⚠ | 0.01405 | 0.01706 |
| 100만 | **0.01968** (최선 460, 멈춤 560) | 0.01744 잘림 | 0.01756 |
| 200만 | 측정 중 | 0.01941 잘림 | 0.01816 |

- **얕은 모델은 단조롭게 오른다**(0.0160 → 0.0182). 데이터를 늘리면 조금씩 좋아진다.
- **수렴시킨 DeepLOB 은 100만 행에서 얕은 모델보다 12% 높다**(0.01968 vs 0.01756).
  "두 모델이 거의 같다" 는 잘린 곡선에서 나온 해석이었다 — 폐기한다.

### ⚠ 50만 행은 조기 종료의 부작용이다

50만 행 DeepLOB 이 양쪽 이웃보다 낮다. "조기 종료 걸림" 으로 기록됐지만, 평가 곡선이
최선(80 epoch) 뒤 일시 하락했다가 **회복하던 중에 인내 10회가 소진돼** 멈췄다
(… 0.0112 → 0.0116 → 0.0121 → 0.0123, 상승 중에 종료). 에폭 예산을 늘려도 같은 자리에서 멈춘다.

**"조기 종료가 걸렸다" 는 "수렴했다" 가 아니다.** 잘림(예산 소진)을 고쳤더니 그 아래에
또 하나가 있었다.

- 지금 도는 200만·500만·1000만 지점도 인내 10 이라 같은 부작용이 있을 수 있다.
  **곡선이 끝나면 이상 지점을 인내 30 으로 다시 잰다.**
- 종목군 분석의 교사는 기동 전에 인내를 30 으로 올렸다(사전등록 부록 D).

---

## KST 00:50 — 시험 운전이 GPU 설정 버그를 잡았다. 그리고 내 GPU 판단이 거꾸로였다

종목군 실행(`group_run.py`)을 종목 36개·30 epoch 로 시험 운전했다. 적재 84/84 블록,
표본 선별, 윈도우 생성까지 정상이었는데 **교사를 `cuda:1` 에 올리는 순간
`CUDA error: invalid device ordinal` 로 죽었다**(`groups/smoke_group_run.fail1_cuda_ordinal.log`).
이대로 본 실행을 걸었으면 적재에 수십 분을 쓰고 같은 자리에서 죽었을 것이다.

### 원인

```
~/.bashrc 82행   export CUDA_VISIBLE_DEVICES=GPU-2c93540d-…   # ollama: GPU1 고정
파이토치가 보는 GPU 수   1
물리 GPU0  GPU-dcd70e26-…  15 MiB    비어 있음
물리 GPU1  GPU-2c93540d-…  49.5 GB   표본 크기 곡선 측정이 사용 중. ollama 서비스도 떠 있음
```

- 사용자 셸 설정이 GPU 를 **물리 GPU1 한 장**으로 고정한다. 여기서 띄운 모든 프로세스는
  GPU1 만 보고 그것을 `cuda:0` 으로 부른다. 그래서 `cuda:1` 은 존재하지 않았다.
- **내 판단이 거꾸로였다.** "곡선이 GPU0 을 쓰니 종목군은 GPU1" 이라 했는데, 곡선은 이미
  물리 GPU1 에 있었고 GPU0 이 비어 있었다. GPU 번호를 확인하지 않고 가정했다.
- **GPU1 은 사용자가 ollama 용으로 고정해 둔 장치다.** 곡선 측정이 모르고 그 장치의 49.5GB 를
  쓰고 있다. 지금 ollama 에 올라간 모델은 없지만, 요청이 오면 부딪힐 수 있다.

### 수정

- `~/.bashrc` 는 사용자 설정이므로 건드리지 않는다.
- 종목군 실행은 **기동할 때만** `CUDA_VISIBLE_DEVICES` 를 물리 GPU0 으로 지정하고 장치는 `"cuda"`.
- **`GROUP_GPU_UUID` 를 받아 보이는 장치의 UUID 와 대조하고, 다르면 즉시 멈춘다.**
  조용히 엉뚱한 GPU 에 올라가 다른 실행과 겹치는 일을 막는다 — 이 프로젝트의 결함은 늘
  "오류 없이 조용히 틀린" 종류였다.

---

## KST 00:55 — 교사는 전체 배치 학습이다. "3일 전체 학습" 은 이대로는 불가능하다

`gpu_eval/deeplob_gpu.py::fit` 을 읽었다:

```python
inputs = torch.tensor(self._standardise(X), dtype=torch.float32, device=self.device)  # 학습 데이터 전체를 GPU 로
def train_step():            # 매 epoch: 전 행에 대해 순전파·역전파 한 번
    latent = self._encode(inputs) ...
```

**미니배치가 없다.** 1 epoch = 전체 행에 대한 경사 한 번. 곡선 측정은 200만 행에서 GPU 49.5GB 를 쓴다.

### 귀결 (메모리 투영은 지금 실측 중 — 곱한 추정으로 확정하지 않는다)

- **곡선의 500만·1000만 지점은 98GB 한 장을 넘어 메모리 부족으로 죽을 가능성이 크다.**
- **지시 1 "3일 전체 학습" (저마찰층 4,570만 행) 은 전체 배치로는 물리적으로 불가능하다.**
- 종목군 분석(200만 행, GPU0 한 장)은 영향 없다.

### 계획

3일 전체를 학습하려면 **미니배치 학습**으로 바꿔야 한다. 짚어둘 것 두 가지:

1. 이것은 모델 구조가 아니라 **학습 절차**의 변경이다. 그런데 "epoch" 의 의미가 바뀐다
   (지금: 1 epoch = 경사 1회 / 미니배치: 1 epoch = 경사 수백~수천 회). 표본 크기 곡선·종목군
   분석의 전체 배치 결과와 **직접 비교되지 않는다.**
2. 그래서 3일 전체에 쓰기 전에 **같은 200만 행·같은 시드에서 미니배치가 전체 배치와 비슷한
   선택 종목 R² 에 닿는지 먼저 검증**한다. 허용 오차는 검증을 돌리기 전에 정한다.

측정: `scale/measure_gpu_memory.py` — 메모리는 값이 아닌 모양에만 달려 있으므로 무작위 배열로
10만~120만 행을 재서 선을 긋고, 200만 행 관측치(49.5GB)와 대조해 투영이 맞는지 확인한다.

---

## KST 01:05 — 시험 운전 성공, GPU 메모리 실측, 그리고 내 수치 하나를 정정한다

### 시험 운전 (GPU0, 종목 36개, 30 epoch) — 통과

```
예측 행      203,581 (평가① 34,838 · 평가② 168,743)
비유한 예측  DeepLOB 0 · 릿지 0 · y 0
복원 재현성  DeepLOB 최대 차이 0.000e+00 · 릿지 최대 차이 0.000e+00
```

1회차는 처음부터, 2회차는 예측값만 지우고 저장된 교사·릿지를 불러와 다시 예측했다.
**차이가 정확히 0 이다** — 재부팅 후 재시작 경로가 조용히 다른 답을 내지 않는다.
(교사 R² 가 음수인 것은 30 epoch 짜리 시험이라 당연하다. 이 운전은 정합성 점검이지 성능 측정이 아니다.)

### 전체 배치 GPU 메모리 실측 — 완벽한 선형

```
행 수      할당 최대   예약 최대
10만        3.85 GB     5.81 GB
20만        7.68        11.66
40만       15.32        23.30
80만       30.62        46.41
```

**10만 행당 약 3.83 GB (할당).** 무작위 배열로 쟀다 — 메모리는 값이 아니라 모양에만 달려 있다.

### ⚠ 정정 — "200만 행에서 49.5 GB" 는 학습 최대치가 아니었다

앞서 곡선 측정이 "200만 행에서 49.5 GB 를 쓴다" 고 적었다. **그것은 학습 최대치가 아니라
어느 단계인지 모르는 순간에 본 nvidia-smi 수치였다.** 실측 선으로 다시 보면:

| 학습 행 | 할당 투영 | GPU 한 장(약 96GB) |
| --- | --- | --- |
| 200만 | **약 77 GB** | 혼자면 들어간다 |
| 500만 | 약 192 GB | 메모리 부족 확실 |
| 1,000만 | 약 383 GB | 불가능 |
| 4,573만 (3일 저마찰 전체) | 약 1.75 TB | 불가능 |

결론은 바뀌지 않는다(곡선의 500만·1000만은 죽고, 3일 전체는 전체 배치로 불가능). 다만
**근거가 이제 한 점을 곱한 추정이 아니라 선형 실측이다.**

### 종목군 본 실행 기동

200만 행 ≈ 77 GB 는 GPU0 한 장에 들어간다(여유 약 20 GB). 시험 운전 통과·인내 30 반영 후 기동했다.
- 물리 GPU0 지정, UUID 불일치 시 즉시 정지
- 판정 규칙 부록 E 점검은 아직 도는 중이지만 기다리지 않는다 — 분석은 저장된 예측값으로
  **언제든 다시 돌릴 수 있게** 설계했다
- `groups/run_detached_stage.sh` 가 단계가 바뀔 때마다 커밋한다

---

## 정정 — 이 문서의 시각 표기 일부가 시계와 달랐다

"KST 00:20" 이후 항목(00:20 · 00:30 · 00:45 · 00:50 · 00:55 · 01:05)은 **시계를 보지 않고 적어**
실제보다 약 7~25분 앞서 있다. 순서는 맞다. **git 커밋 시각이 기준이다**
(`2a4d076` 00:13 · `a5be63a` 00:16 · `b4d8384` 00:31 · `1a32163` 00:35 · `1a49788` 00:36 · `41ffe39` 00:40).
사전등록 문서의 같은 정정은 `PREREG-GROUPS.md` 끝에 있다. **이후 시각은 `date` 명령 값으로만 적는다.**

## 2026-09-14 KST 00:43 (date 명령 값) — 부록 E 채택, 곡선 200만 행이 GPU1 을 거의 다 채운다

**부록 E 채택** — 적대적 점검 전부 통과(신호 없음 6회 거짓 경보 0회, 교란·약한 교란 전부 검출).
판정 규칙을 실측 전에 세 번(B·C·E) 고쳤고, 세 번 모두 알려진 답이 있는 합성 데이터가 결함을 드러냈다.
사전등록 원문을 그대로 실측에 썼다면 세 결함이 모두 결과에 섞였을 것이다.

**곡선 200만 행** — GPU1 사용 93,031 MiB / 약 98 GB, 사용률 100%, UTC 15:24 부터 학습 중.
메모리 실측 투영(할당 76.5 GB · 예약 115.9 GB)과 맞게 한 장을 거의 다 채운다.

- 종목군 실행(GPU0, 같은 200만 행)이 한 장에 들어간다는 **실측 근거**다. 곡선이 이 지점을 끝내면 확정.
- **GPU1 은 사용자가 ollama 용으로 고정한 장치다.** 93 GB 가 차 있어 지금 ollama 는 모델을 올리지
  못한다. 곡선의 다음 지점(500만 행, 투영 191 GB)은 메모리 부족으로 죽을 것이므로 곧 풀린다.

---

## 2026-09-14 KST 00:48 (date 명령 값) — 곡선 200만·500만 결과, 설계 문서와 미니배치 사전등록

### 표본 크기 곡선 (안 본 종목 R², 수렴 기준)

| 학습 행 | DeepLOB (GPU, 예산 1500) | 얕은 모델 (CPU) |
| --- | --- | --- |
| 20만 | 0.01788 | 0.01595 |
| 50만 | 0.01405 ⚠ 조기 종료 부작용 | 0.01706 |
| 100만 | 0.01968 | 0.01756 |
| 200만 | **0.02132** (학습 468초, 최선 340, 멈춤 440) | 0.01816 |
| 500만 | 측정 중 — 전체 배치 메모리 부족 예상(투영 191 GB) | **0.01880** (학습 1,595초, 최선 480, 멈춤 580) |
| 1000만 | — | 측정 중 |

- **얕은 모델:** 25배(20만 → 500만) 늘려 +18%. 로그처럼 느리게 오른다.
- **DeepLOB:** 10배(20만 → 200만) 늘려 +19%. 같은 200만 행에서 얕은 모델보다 +17%.
- **절대 수준은 여전히 0.02 대다.**
- **200만 행 전체 배치가 98 GB 한 장에서 메모리 부족 없이 끝났다.** 같은 200만 행인 종목군 실행이
  GPU0 한 장에 들어간다는 실측 확인이다.

### 종목군 실행

적재 5,757 / 5,763 블록(6개는 해당 종목·날짜 파일 없음), 공통 특징 19, 213초.
학습 풀 30,353,580행, 선택 풀 3,754,989행. 표본 선별 중.

### 설계 문서와 미니배치 사전등록

- `DESIGN.md` — 실측을 맡았던 에이전트가 세션과 함께 죽어 없던 설계 문서를, 실제로 설계·구현된 대로 썼다.
- `PREREG-MINIBATCH.md` — 지시 1 "3일 전체 학습" 은 전체 배치로 불가능하므로 미니배치로 간다.
  **미니배치 코드를 한 줄도 쓰기 전에** 커밋한다. 핵심 두 가지:
  1. 3일 학습 전에 **같은 200만 행에서 미니배치가 전체 배치 선택 R² 의 90% 이상**에 닿는지 먼저 본다.
     실패하면 조정하며 다시 돌리지 않고 실패를 보고한다.
  2. 3일 전체 vs 200만 행 비교는 **같은 평가 행에서 종목 단위 짝지은 부트스트랩**으로 차이의 구간을 낸다
     — 부록 C 에서 배운 대로, 서로 독립이 아닌 두 R² 를 따로 구간 추정해 비교하지 않는다.

---

## 2026-09-14 KST 00:59 (date 명령 값) — 종목군 첫 실행 메모리 부족·재기동, 곡선 500만 메모리 부족, 미니배치 코드

### 종목군 실행 — 첫 학습 단계에서 GPU 메모리 부족

- 표본 선별까지 정상: 학습 1,979,970행 · 선택 197,998행 · 윈도우 (1979970, 304), 366초. GPU 확인 통과.
- 첫 역전파에서 메모리 부족으로 종료(UTC 15:52:35). 오류 문구: 할당 33.23 GB, **예약됐지만 안 쓰는 조각
  37.77 GB**, 42.51 GB 요청 실패, 남은 공간 23.27 GB.
- **총량이 아니라 조각화 문제다.** 실측 투영으로 200만 행 최대 할당은 76.5 GB, 한 장은 95 GB 다.
  같은 크기의 곡선 200만 지점은 GPU1 에서 끝났다.
- 조치: 파이토치 메모리 할당 방식 설정 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 로 재기동
  (KST 00:58:12). 메모리를 잡는 방식만 바뀌고 모델·데이터·시드·계산 값은 그대로다.
  사전등록 항목은 하나도 바뀌지 않는다.
- 재기동 전 점검 `groups/check_fit_memory_2m.py`: 같은 모양의 무작위 배열·같은 설정으로 12 epoch
  (4 epoch 마다 선택 평가를 끼워) 돌려 최대 메모리를 잰다. 실제 실행은 적재·선별에 약 10분이 걸려
  그 전에 끝난다. 점검이 실패하면 실제 실행을 GPU 단계 전에 멈춘다.
- 적재·선별은 다시 한다(체크포인트가 학습 이후부터만 있다). 약 10분 손실.

### 곡선 DeepLOB 500만 행 — 예상대로 메모리 부족

- 106.27 GB 요청 실패. 메모리 실측 투영(할당 191 GB)대로 전체 배치로는 한 장에 안 들어간다.
  **곡선 DeepLOB 은 200만 행까지 4개 지점으로 확정.**
- 감시 스크립트의 종료 줄은 `exit=0` 이지만 로그의 오류 추적이 사실이다. 같은 스크립트를 얕은 곡선
  감시가 아직 읽는 중이라 고치지 않고 기록만 한다.
- **GPU1 에는 지금 ollama 가 모델을 올려 두었다**(llama-server, UTC 15:54 시작). GPU1 은 사용자의
  ollama 용이므로 앞으로 무거운 작업은 GPU0 에서만 한다.

### 미니배치 코드 (사전등록 커밋 75a21e6 이후 작성)

- `groups/minibatch_teacher.py` — 학습 절차만 바꾼다. 모델 구조·초기화·손실·예측 함수는 기존 교사
  인스턴스 것을 그대로 쓴다. 윈도우는 묶음마다 원본에서 만들고 전처리는 한 번만 한다. 조기 종료는
  기존 함수 그대로, 평가 한 번 = 0.25 epoch, 인내 20회.
  - 확인한 사실: 기존 조기 종료의 "인내"는 epoch 수가 아니라 **평가 횟수**다. 종목군 실행은 인내 30 ×
    10 epoch 마다 평가 = 300 epoch. 미니배치는 인내 20회 × 0.25 epoch = 5 epoch — 사전등록과 일치.
- `groups/paired_analysis.py` — 짝지은 부트스트랩. 합성 점검 전부 통과(커밋 9d2811f).
- `groups/test_minibatch_teacher.py` 첫 실행:

  | 점검 | 결과 |
  | --- | --- |
  | 즉석 윈도우 = 기존 윈도우 함수 (비유한 값·세션 경계 포함) | 비트 단위 동일, 통과 |
  | 흘려 계산한 표준화 통계 = 전체 배치 방식 (상수 열 16개 척도 정확히 1) | 통과 |
  | 학습 뒤 최선 시점 상태로 되돌림 | 통과 |
  | 섞은 y 로는 못 배움 | 선택 R² 0.0018, 통과 |
  | **알려진 신호를 배움 (선택 R² > 0.3)** | **실패: 0.0159** (이론 상한 약 0.85) |

  6 epoch 끝까지 최선이 갱신 중이었다. 학습 부족인지 결함인지 이것만으로는 가를 수 없다.
  진단: 같은 합성 데이터에서 기존 전체 배치 학습기와 비교하고, 미니배치를 40 epoch 돌린 궤적을 본다.
  **진단이 끝나기 전에는 1단계 동등성 검증에 쓰지 않는다.** 이 점검은 학습기 자체의 결함 확인이다.
  1단계의 학습률·묶음 크기는 사전등록대로 고정이고 이 진단 결과로 바꾸지 않는다.

**재기동 전 메모리 점검 결과 (KST 01:00, date 명령 값) — 통과.** 같은 모양·같은 설정으로 12 epoch:
최대 할당 **75.74 GB**, 최대 예약 **77.81 GB** / 한 장 94.97 GB, 47초. 설정 전 투영 예약 116 GB 가
할당과 거의 같아졌다(조각이 사라짐). 여유 약 17 GB. 파이토치가 설정 이름이 바뀌었다는 경고를 냈지만
예약이 할당과 같아진 것이 설정이 먹었다는 증거다. 원자료 `groups/data/check_fit_memory_2m.json`.

---

## 2026-09-14 KST 01:08 (date 명령 값) — 미니배치 학습 점검 실패의 진단: 결함이 아니라 정체 구간

같은 합성 데이터(이론 상한 R² 약 0.85)에서 조기 종료를 끄고 길게 돌렸다(`groups/diag_minibatch_plateau.py`, 로그 같은 이름).

| 학습기 | 최선 선택 R² | 궤적 |
| --- | --- | --- |
| 전체 배치 (기존, lr 1e-2, 600 epoch) | **0.840** | 50 epoch 마다: −0.08, 0.01, −0.03, **0.81**, 0.84 … |
| 미니배치 (lr 1e-3, 40 epoch) | **0.834** | epoch 마다: 1~15 epoch 동안 0.013~0.019 에 머물다 16~17 epoch 에 0.43 → 0.68 → … 0.83 |

- **두 학습기 모두 긴 정체(R² 약 0.02) 뒤에 갑자기 오른다.** 첫 점검의 6 epoch 는 정체 안에서 끝났다.
  학습기 결함이 아니다. 끝까지 가면 미니배치가 전체 배치의 99% 에 닿는다.
- 점검 예산을 40 epoch·조기 종료 끔으로 올려 다시 돌린다. 바꾼 것은 **점검의 예산**뿐이다 — 학습기 코드와
  1단계 설정(학습률 두 후보·묶음 8,192·인내 20회·상한 30 epoch)은 그대로다.

### 1단계에 대한 위험 — 결과 보기 전에 적어 둔다

실제 데이터에도 이런 정체가 있으면, 사전등록한 **인내 20회(= 5 epoch)가 정체 중에 학습을 멈출 수 있다.**
그러면 1단계가 "미니배치가 못 배운다" 가 아니라 "조기 종료가 정체에서 멈췄다" 는 이유로 실패할 수 있다.

- 사전등록 §2·§6 대로 **인내를 바꿔 다시 돌리지 않는다.**
- 1단계가 실패하면 원인 진단에서 평가 궤적을 본다: 멈춘 시점까지 선택 R² 가 평평했는가(정체에서 멈춤),
  아니면 올랐다가 내려왔는가(과적합). 진단 결과는 보고하되 판정을 뒤집지 않는다.
- 참고로 전체 배치 곡선의 50만 행 지점(인내 10)이 같은 종류의 조기 종료 부작용이었다(부록 D).

### 종목군 실행

재기동 뒤 적재 171초, 표본 선별 245초, **GPU 확인 통과(UTC 16:05:51) — 교사 학습 중.** GPU0 80.4 GB.
미니배치 실행 코드(`groups/minibatch_run.py`: 1단계 `gate`, 2단계 `full`)와 소규모 시험 운전
(`groups/smoke_minibatch_run.py`)을 작성했고 시험 운전을 GPU0 에서 돌리는 중(메모리 2 GB 미만).

- 1단계는 종목군 실행과 **같은 순서로 적재·같은 시드로 선별**해 같은 행을 뽑는다. 이를 두 가지로 확인하고
  다르면 멈춘다: 학습·선택 행 수, 그리고 학습 행 윈도우의 표준화 평균·척도가 종목군 교사 저장본과 같은가.
- 음성 대조군 판정은 `group_analysis.verdict` 를 그대로 부른다(규칙을 옮겨 쓰지 않는다).
- 사전등록 해석 하나: §1 표의 "학습률 두 후보 중 선택 종목 R² 로 선택" 은 미니배치 학습 절차이므로
  **2단계에서도 두 후보를 모두 학습하고 고른다.** 1단계에서 고른 학습률을 물려받지 않는다.

**미니배치 실행 시험 운전 결과 (KST 01:12, date 명령 값) — 전부 통과** (`groups/smoke_minibatch_run.log`).
36종목 축소판, 미니배치는 묶음 8,192 그대로·상한 1 epoch.

| 점검 | 결과 |
| --- | --- |
| 1단계 학습·선택 행이 기준(전체 배치) 실행과 같다 | 행 수 19,812 · 4,952 같음, 표준화 평균 차이 0, 척도 상대 차이 1.2e-13 |
| 저장된 교사를 불러와 다시 예측 | 203,581행 최대 차이 0 |
| 1단계 판정·짝지은 비교가 나온다 | 나온다. 기준값이 0 이하인 경우를 표시한다 |
| 2단계가 3일(16·17·18)로 학습하고 행이 늘어난다 | 507,311행 > 19,812행, 판정 문자열 나옴, 음성 대조군 위반 0 |

시험 운전의 R² 값(−14, −4 등)은 **의미 없다** — 수천 행을 몇 묶음만 학습했다. 이 값은 학습이 거의 안 된
교사의 출력 크기가 y 보다 훨씬 커서 R² 가 크게 음수가 된다는 것만 보여 준다. 실제 1단계는 첫 평가(0.25 epoch)
전에 약 60 묶음을 학습한다.

**진단 추가분 (KST 01:13, date 명령 값):** 진단 스크립트는 미니배치 학습률 3e-3 도 돌렸다 — 최선 **0.8406**(35.5 epoch),
10 epoch 동안 0.02 부근 정체 후 급등. **3e-3 은 진단용일 뿐 1단계 후보가 아니다.** 1단계 후보는 사전등록대로
{1e-3, 3e-4} 그대로이며, 이 진단 결과를 보고 후보를 바꾸지 않는다. 로그 사본(`groups/diag_minibatch_plateau.log`)을
이 줄까지 들어간 전체본으로 교체했다(첫 사본은 3e-3 이 끝나기 전에 복사됐다).

**학습기 점검 재실행 (KST 01:14, date 명령 값) — 전부 통과** (`groups/test_minibatch_teacher.log`).
진짜 y: 선택 R² **0.834**(40 epoch). 섞은 y: **0.0018** — 가짜로 점수를 내지 않는다. 윈도우 비트 단위 동일,
표준화 통계 동일, 최선 시점 복원도 통과. 미니배치 코드의 실측 전 점검(학습기·짝지은 비교·실행 시험 운전)이 모두 끝났다.

---

## 2026-09-14 KST 01:15 (date 명령 값) — 표본 크기 곡선 재측정 결정 (결과 보기 전)

**확인한 사실:** 지금까지의 곡선은 DeepLOB 4개 지점·얕은 모델 5개 지점 **모두 인내 10**(10 epoch 마다 평가)으로 쟀다
(각 결과 JSON 의 `patience` 필드). 종목군 실행은 부록 D 로 인내 30 이다. 50만 행 DeepLOB 0.01405 는 인내 10 이
일시적 하락 구간에서 멈춘 부작용으로 판단했었다.

**결정:** 인내 30 으로 **곡선의 모든 지점을 다시 잰다** — 부작용이 보인 50만 행 한 점만 다시 재면, 나빠 보이는 점만 골라
고치는 것이 된다. 인내 10 결과는 지우지 않고 나란히 보고한다.

| 모델 | 지점 | 장치 | 시작 조건 |
| --- | --- | --- | --- |
| DeepLOB | 20만·50만·100만·200만 (500만 이상은 전체 배치 메모리 부족 확정) | GPU0 | 종목군 실행이 끝나 GPU0 이 빈 뒤 |
| 얕은 모델 | 20만·50만·100만·200만·500만 (+1000만은 시간이 되면) | CPU | 지금 도는 인내 10 의 1000만 지점이 끝난 뒤 |

나머지 설정(학습 예산 DeepLOB 1500·얕은 모델 700 epoch, 10 epoch 마다 평가, 시드, 종목·날짜)은 그대로다.
**예상:** 50만 행 DeepLOB 은 0.014 → 20만·100만 사이 값(0.018~0.020)으로 올라온다. 다른 지점은 거의 그대로다
(대부분 최선 epoch 뒤 100 epoch 동안 개선이 없어 멈췄으므로, 인내 30 이면 300 epoch 더 보게 될 뿐이다).

---

## 2026-09-14 KST 01:25 (date 명령 값) — 종목군 교사·릿지 학습 완료, 1단계 기동

### 종목군 실행 학습 결과 (선택 종목 = 학습에 안 쓴 종목, 20260318)

| | 선택 종목 R² | 비고 |
| --- | --- | --- |
| DeepLOB 교사 (전체 배치, 200만 행) | **0.02283** | 학습 1,030초, 최선 700 epoch, 1000 epoch 에서 조기 종료(인내 30 소진) |
| 릿지 (같은 304차원 윈도우 입력, 같은 행·가중치) | **0.02360** | 규제 0.001(후보 중 가장 약한 값), 117초 |

- **선형 릿지가 DeepLOB 보다 선택 종목에서 조금 높다.** 선택 종목 R² 는 조기 종료·규제 선택에 쓴 값이라 최종 성적이
  아니다. 최종 성적은 평가 ① (안 본 종목·안 본 날)에서 나온다. 다만 "DeepLOB 이 선형 모델이 못 잡는 것을 잡는다" 는
  전제가 이 데이터에서 약하다는 첫 신호다.
- 릿지 규제가 후보의 가장 약한 끝(0.001)에서 골라졌다 — 더 약한 값이 더 나았을 수 있다. 사전등록 부록 A4 의 후보
  범위는 바꾸지 않는다. 규제가 거의 없는 쪽이 최선이라는 것은 200만 행이면 304차원 선형 모델이 과적합하지 않는다는 뜻이다.
- 교사 궤적(50 epoch 마다): −1.75, 0.004, 0.015, 0.020, 0.021, 0.022 … 0.0227(최선 부근) … 0.0220. 곡선의 인내 10
  측정(200만 행 0.02132, 최선 340)보다 **인내 30 이 더 오래 학습해 더 높다(+7%)** — 선택 종목·날짜 구성이 곡선과
  달라 직접 비교는 아니지만, 재측정 결정(인내 30)의 방향과 맞다.

### 1단계 (미니배치 동등성) 기동

기준값이 생겼으므로 GPU0 에서 1단계를 띄웠다(`groups/minibatch_run.py gate`, 로그 `groups/mb_gate.log`).
종목군 실행은 테스트일 예측 중이고 GPU0 에 80 GB 를 잡고 있다. 1단계 미니배치는 5 GB 미만이라 함께 돈다.
**통과 기준(사전등록 §2): 미니배치 선택 R² ≥ 0.02283 × 0.9 = 0.02055.**
