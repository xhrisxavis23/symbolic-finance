# 0914 진행 기록

## 2026-09-14 KST 15:18 — 폴더 개설, 사전등록

지시(KST 15:06): 교사 모델 성능을 R² 가 아니라 **수익이 나는가**로 먼저 따진다. 1·2단계 모두 진행,
md 기록, HTML 보고서 수정 후 GitHub 반영, 30분마다 점검.

**실행 전에 확인한 것 (코드·기록 읽기만, 손익 수치는 계산하지 않음):**
- 교사 자체 손익은 한 번도 잰 적이 없다 — `0910/PREREG.md:32` 에서 범위 밖으로 뺐다.
- 교사 게이트(`0902/sd/teacher/gate.py`)에 경제성 항목이 없다.
- `y_path` = (30초 뒤 BID1 / 진입 ASK1 − 1)·1e4 − 23 을 (스프레드 + 23) 으로 나눈 값 → 이익은 `y_path > 0` 꼬리.
- 진입 임계는 직전 100틱 분위수라 교사의 손익분기(0)를 쓰지 않는다.
- 정본 재생은 `ProcessPoolExecutor`(fork) 워커가 종목-일마다 `framework.data.load` 로 적재하고, 진입식 입력과 분위 임계를
  모두 `ExpressionRuntime.evaluate` 로 계산한다 → vendor 수정 없이 실행 프로세스에서 두 함수만 감싸 교사 점수를 주입할 수 있다.
- 3일 교사 선택 학습률 1e-3 (`mb_full/progress.json`), 평가 ① 유효 행 3,849,436 · 평가 ② 15,569,153.
- 진행보드(onchain) 편집 비밀번호가 환경에 없다 → 보드 게시는 하지 않는다. "허브" 반영은 GitHub `xhrisxavis23/symbolic-finance` 로 한다.
- 서버 공용: 다른 사용자 프로세스가 CPU 12코어·GPU1 을 쓰고 있다. 교사 재예측은 GPU0(UUID `GPU-dcd70e26…`) 을 쓴다.

**사전등록:** `PREREG-TEACHER-ECON.md` — 교사 3 × 규칙 4, 1단계 라벨 기준 걸러보기, 2단계 정본 계약 판정,
실행 전 점검 C1~C4, 사전 예측 5개. **이 커밋 전에는 실험 코드가 없다.**

**점검 일정:** 매시 :06 · :36 (세션 cron).

## 2026-09-14 KST 15:33 — 1단계 실행 완료(결과 미열람) · C1 실패 → 진단 → 정정(부록 A)

**실행:**
- 1단계 `stage1/label_economics.py` 완료(KST 15:26 기동, 약 2분). 분위 자기 점검(grid vs 단일 함수) 최대 차 0. **결과 파일은 C1 정정을 커밋할 때까지 열지 않는다.**
- `stage2/make_scores.py` (GPU0) 411종목 3,854,714틱 점수 생성 154초. 건너뜀 0 · 정렬 실패 0.

**C1 실패:** RDG 1.8e-15 통과, T2M 5.8e-3 · T3D 1.67e-2 로 기준 1e-5 초과. C3·C4 는 게이트에서 멈췄다(의도대로).

**진단 (`stage2/diag_c1.py`, 12종목, y 미사용):** 같은 GPU 에서 같은 행을 묶음 크기만 바꿔도 T2M 9.7e-4 · T3D 4.1e-3,
TF32 를 끄면 1.9e-3 · 2.7e-3 달라진다. 같은 묶음 반복은 0. 새 점수 vs 저장 예측 차이(1.5e-3 · 3.5e-3)가 이 자기 잡음과 같은 크기이고,
`score > 0` 판정 100% · Q95 신호 99.97~99.98% 일치. 0911 은 100종목 이어 붙인 50만 행 묶음(cuDNN TF32 기본 켜짐), 이번에는 종목별 한 묶음.
→ 모델·특징·정렬 오류가 아니라 **GPU 순전파의 묶음 모양 의존 잡음**. 1e-5 는 GPU 자신도 못 지키는 기준이었다(내 사전등록 잘못).

**판단 R-0914-1 (C1 정정):** 원문은 두고 `PREREG-TEACHER-ECON.md` 부록 A 로 C1′ 을 정한다 — 정렬 완전 일치 · RDG ≤ 1e-5 ·
DeepLOB 규칙 4개 신호 일치 ≥ 99.5% · 교사를 맞바꾸면 Q70 일치 < 99.5%(판별력 음성 대조). 교사·점수·계약·판정·사전 예측은 그대로.
2단계 재생 전, 1단계 결과 열람 전에 커밋한다.

## 2026-09-14 KST 15:36 — 1단계 결과 열람 · 2단계 점검 연쇄 기동

**1단계 판정 (평가 ①, 본페로니 99.58%):** 라벨 기준 이익 **0** · 손실 **10** · 신호 부족 **2**.
- **DeepLOB 두 교사는 안 본 종목 3,849,436행 중 한 행에서도 `y_path > 0` 을 예측하지 않았다**(최댓값 T2M −0.037, T3D −0.070) → ABS 신호 부족.
- 분위 규칙 9개 실제 `y_path` 평균 −0.853 ~ −0.910 (전체 행 −1.002). 이익 비율 4.05~4.63% (전체 3.75%).
- 보정은 좋다 — 구간마다 평균 예측 ≈ 실제 평균, 단조 증가. T3D 상위 0.1% 도 실제 −0.405 (이익 비율 30%).
- 릿지 ABS 1,173행은 외삽 폭주(예측 최댓값 17.3), 실제 −1.24.
- 사전 예측 1 맞음, 2 절반쯤 틀림(손실 아닌 신호 부족 2개). 전문: `RESULTS-STAGE1.md`.

**2단계:** C1′ → C3·C4 → 스모크(3종목, 구조만) 순서로 연쇄 기동(KST 15:35). 앞 단계가 실패하면 뒤는 돌지 않는다.
**2단계에 대한 함의(판정 아님):** DeepLOB ABS 계약은 유효 행 기준 신호가 없으므로 정본 재생에서도 거래가 거의 없을 것이다.

## 2026-09-14 KST 15:37 — 실행 전 점검 전부 통과 · 본 재생 기동

| 점검 | 결과 |
| --- | --- |
| C1′ (부록 A.3) | **통과** — 정렬 411종목 완전 일치 · RDG 1.8e-15 · 같은 교사 신호 일치 ABS 100% / Q70 99.95~99.96% / Q85 99.96~99.97% / Q95 99.98% · 교사 맞바꾸면 Q70 84.4% (걸러냄) |
| C4 신호 독립 대조 | **통과** 105/105 (5종목 × 교사 3 × ABS + Q 3개 × 단일·공유 경로) |
| C3 주입 경로 동등성 | **통과** — 가짜 교사(점수 = book_imbalance) 원장 = 나이브 원장, 행 단위 동일 (20종목, q 0.70/0.85/0.95 = 5,297 / 3,808 / 2,270행), 오류·결측 0 |
| C2 틱 정렬 | 워커 안에서 매 종목-일 대조 — C3·스모크에서 위반 0 |
| 스모크 (3종목 × 27계약, 구조만) | 오류 0 · 결측 0 · 22초. ABS 계약 3개는 원장 행 0 (신호 없음 — 1단계와 일치) |

**본 재생 기동:** 411종목 × 27계약, 워커 32 (`logs/replay_main.log`).

### 2026-09-14 KST 15:39 — 정기 점검
- 1단계 완료 · 실행 전 점검 C1′·C3·C4·스모크 통과. 본 재생 진행 중 (프로세스 37 개, 경과 01:53, 원장 아직 미기록 — 끝날 때 한 번에 쓴다). 서버 부하: 111.92, 73.04, 58.93.

## 2026-09-14 KST 16:12 — 2단계 완료: 교사 자체도 정본 계약에서 벌지 못한다

**본 재생:** KST 15:37:37 → 16:02:04 (재생 1,436초), 411종목 × 27계약, 원장 1,902,700행(체결 61,150), 결측 0 · 오류 0.

**판정 (교사 계약 12개, 본페로니 99.58%):** 수익 **0** · 손실 9 · 거래 부족 2 (DeepLOB ABS, 결정 0) · 판정 불가 1 (릿지 ABS, 체결 19).
- 분위 계약 의사결정당 −0.605 ~ −0.797bp, 체결당 net −18.7 ~ −25.3bp. Q95 체결당 gross: T3D +4.3 [+1.0, +7.7] · 릿지 +3.3 · T2M +1.9.
- 비교(서술): T3D − 나이브 Q85 +0.12 · Q95 +0.19 (구간 0 위). **T3D − 0910 시드 1·2 수식은 세 q 모두 구간이 0 포함**, T2M · 릿지 Q95 는 시드 2 보다 조금 나쁨(−0.19 · −0.12).
- 체결률 2.5~3.8%, 방향 오류(PERSISTENT_ADVERSE) 66~78%.

**사전 예측:** 1 맞음 · 2 절반쯤 틀림 · 3 수익 없음은 맞음(거래 0 인 2개는 총 순손익 0 이라 "모두 < 0" 은 문자 그대로 틀림) · 4 맞음 · 5 릿지에서 틀림.

**해석:** 교사와 증류 수식의 손익이 구별되지 않는다 → 병목은 증류가 아니라 교사 신호의 크기. 손익분기 규칙은 거래가 없고, 분위 규칙은 교사가 손실로 예측한 틱을 상대적으로 덜 나쁘다는 이유로 산다.
전문: `RESULTS-STAGE2.md`. 다음: EXPERIMENT-RECORD · README · 0911 보고서 HTML 갱신 → GitHub 반영.

## 2026-09-14 KST 16:17 — 기록·보고서 갱신

- `RESULTS-STAGE2.md` 작성, `README.md` 에 결과·산출물 표.
- 최상위 `EXPERIMENT-RECORD.md` 에 §5b(0914) 추가, §0 한 줄 결론·단계 표, §6 결론, §8.2 판단 오류(C1 기준 · 사전 예측 2·5), §9 남은 질문(계약 손익에 맞춘 학습 목표), §10 문서 지도 갱신.
- 최상위 `README.md` 에 0914 단계·결과·폴더 구조 추가.
- **0911 보고서 HTML** (`0911/symbolic_distillation_0911_report.html`, `board/build_report.py` 로 재생성): 맨 앞에 "교사 자체는 돈을 버는가 (0914)" 절 — 두 단계 설계 · 실행 전 점검 · 1단계 표와 보정표 · 2단계 판정표 · 같은 q 비교 · 사전 예측 대조. 제목 · 요약 · 목차 · 종합 판정 ⑧ · 남은 질문 · 자료 위치 · 한계 갱신. 빌드 점검: 절 13개 · 목차 링크 20개 전부 연결 · 태그 짝 정상. 0911 저장소에 커밋.
- 진행보드(onchain) 본문 `board/progress_body.md` 에도 0914 요약을 넣었지만 **보드 게시는 하지 않았다**(편집 비밀번호 없음).
- 다음: GitHub `xhrisxavis23/symbolic-finance` 반영.
